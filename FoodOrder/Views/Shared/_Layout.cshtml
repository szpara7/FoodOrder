<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Order</title>
    <link href="https://fonts.googleapis.com/css?family=Anton|Nosifer|Indie+Flower" rel="stylesheet" />
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/font-awesome.min.css" rel="stylesheet" />
    <link href="~/Content/ownCss.css" rel="stylesheet" />
    <script src="~/Scripts/jquery-3.1.1.min.js"></script>
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
    <script src="~/Scripts/modernizr-2.6.2.js"></script>
</head>

<body>

    <div class="ownContainer">

        @*<div class="ownNavbar row">
                <div class="container-fluid">
                    <div class="col-lg-12">
                        <div class="col-lg-8">
                            <a class="navbar-brand" href="@Url.Action("Index","Home")">
                                FoodOrder
                            </a>
                        </div>
                        <div class="col-lg-2" id="shoppingCart">
                            @if (!Html.UserHasRole("Admin", "Seller", "Cook"))
                            {
                                @Html.Action("ShowPartialCart", "Cart")
                            }
                        </div>
                        <div class="col-lg-2">
                            @Html.Action("Index", "Auth")
                        </div>
                    </div>
                </div>
            </div>*@

        <div class="ownNavbar row">

            <div class="col-md-2 navbarBrand col-xs-12">
                <a style="text-decoration:none" href="@Url.Action("Index","Home")">
                    <h4 class="logo">Food <br />Order</h4>
                </a>
            </div>
       
                <div class="col-md-offset-6 col-md-2 col-xs-6 shoppingCart" id="shoppingCart">
                    @if (!Html.UserHasRole("Admin", "Seller", "Cook"))
                    {
                        @Html.Action("ShowPartialCart", "Cart")
                    }
                </div>
                <div class="col-md-2 col-xs-6">
                    @Html.Action("Index", "Auth")
                </div> 

        </div>

        <div class="row content">
            <div class="col-md-2 categories">
                <h1><b>Categories</b></h1>
                @Html.Action("CategoriesList", "Home")
            </div>

            <div id="renderBody" class="col-md-10">
                @RenderBody()
            </div>
        </div>

    </div>

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
</body>
</html>